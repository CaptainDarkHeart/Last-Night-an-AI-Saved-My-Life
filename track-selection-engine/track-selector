#!/usr/bin/env python3
"""Track Selector CLI wrapper."""

import sys
from pathlib import Path

# Add src directory to path
src_path = Path(__file__).parent / "src"
sys.path.insert(0, str(src_path))

from track_selector.cli import main

if __name__ == '__main__':
    main()
